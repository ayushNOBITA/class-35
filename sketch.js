var gameState=0
var playerCount;
var form,player,game;
var database,canvas

function setup(){
    database=firebase.database();
    canvas=createCanvas(500,500);
   
}

function draw(){
   
}

